package com.channel360.workflow.application.designer.validation;

public enum ValidationPhase {
    STRUCTURE,
    REFERENCES,
    GRAPH,
    TRANSITIONS,
    ASSIGNMENTS,
    CONDITIONS,
    PUBLISH
}
